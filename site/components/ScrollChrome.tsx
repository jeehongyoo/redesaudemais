"use client";

import { useEffect } from "react";

/** Barra de progresso horizontal (fio lateral pontilhado removido — era ruído de template SaaS) */
export function ScrollChrome() {
  return <div id="read-progress" aria-hidden="true" />;
}
